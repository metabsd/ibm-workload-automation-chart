{{- if .Capabilities.APIVersions.Has "route.openshift.io/v1" -}}
{{- if or (.Values.server.routes.enabled) (eq (.Values.server.exposeServiceType | upper )  "ROUTE")  }} 
####################################################################
# Licensed Materials Property of HCL*
# (c) Copyright HCL Technologies Ltd. 2019. All rights reserved.
#
# * Trademark of HCL Technologies Limited
####################################################################
{{- $routeType := .Values.server.routes.routeType | default .Values.global.routeType | default "passthrough" }}
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: wa-server-route
  labels:
{{ include "wa.server.labels" . | indent 4 }}
  annotations:
    openshift.io/host.generated: 'true'
    haproxy.router.openshift.io/cookie_name: wa-console-affinity-cookie
spec:
  host: 
  to:
    kind: Service
    name: {{ template "waMdm.ServiceName" . }}
    weight: 100
  port:
    targetPort: server-https
  tls:
    termination: {{ $routeType }}
    {{- if ne ($routeType | upper) "PASSTHROUGH" }}
    insecureEdgeTerminationPolicy: None
    {{- end }}
    {{- if eq ($routeType | upper) "REENCRYPT" }}
    {{- $secret := lookup "v1" "Secret" .Release.Namespace "ca-key-pair" }}
    {{- if and $secret (hasKey $secret.data "tls.crt") }}
    destinationCACertificate: |
{{ index $secret.data "tls.crt" | b64dec | indent 6 }}
    {{- end }}
    {{- end }}
  wildcardPolicy: None
status:
  ingress:
    - host: 
      routerName: default
      conditions:
        - type: Admitted
          status: 'True'
      wildcardPolicy: None
{{- if  (.Values.server.enableSingleInstaceNetwork  )  }} 
{{- $root := . -}}
{{range $i, $e := until (.Values.replicaCount |int )}}
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: "s{{ $i }}"
  labels:
{{ include "wa.server.labels" $root | indent 4 }}
  annotations:
    openshift.io/host.generated: 'true'
    haproxy.router.openshift.io/cookie_name: wa-console-affinity-cookie
spec:
  host: 
  to:
    kind: Service
    name: {{ template "waMdm.ServiceName" $root }}-{{ $i }}
    weight: 100
  port:
    targetPort: server-ssl-eif
  tls:
    termination: {{ $routeType }}
    {{- if ne ($routeType | upper) "PASSTHROUGH" }}
    insecureEdgeTerminationPolicy: None
    {{- end }}
    {{- if eq ($routeType | upper) "REENCRYPT" }}
    {{- $secret := lookup "v1" "Secret" .Release.Namespace "ca-key-pair" }}
    {{- if and $secret (hasKey $secret.data "tls.crt") }}
    destinationCACertificate: |
{{ index $secret.data "tls.crt" | b64dec | indent 6 }}
    {{- end }}
    {{- end }}
  wildcardPolicy: None
status:
  ingress:
    - host: 
      routerName: default
      conditions:
        - type: Admitted
          status: 'True'
      wildcardPolicy: None
{{- end }}  
{{- end }}  
{{- end }} 
{{- end }}  